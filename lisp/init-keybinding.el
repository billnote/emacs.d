;;; init-keybinding.el --- Summary
;;; Commentary:
;;; init key binding
;;; Code:

;; global key binding
(global-set-key (kbd "C-S-SPC") nil)

;; dired mode key binding
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; (define-key dired-mode-map (kbd "f-o") 'xah-open-in-gimp)        ;;
;; (define-key dired-mode-map (kbd "f-s") 'xah-dired-scale-image)   ;;
;; (define-key dired-mode-map (kbd "f-p") 'xah-dired-2png)          ;;
;; (define-key dired-mode-map (kbd "f-j") 'xah-dired-2jpg)          ;;
;; (define-key dired-mode-map (kbd "f-m") 'xah-dired-show-metadata) ;;
;; (define-key dired-mode-map (kbd "f-l") 'xah-dired-sort)          ;;
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;

(provide 'init-keybinding)
;;; init-keybinding.el ends here
